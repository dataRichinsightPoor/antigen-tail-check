# Antigen-tail Check acceptance inventory

## Numerical correctness

- Analytic equal-mean mixture: means 50,000; tails 0%/20%; global D 80 pp; Hill unresponsive share 0.159744%/20.051241%.
- CDF crossing reverses tail ordering at a 60,000 cutoff.
- Identical distributions produce zero CDF separation and equal response.
- Exact zeros, ties, merged counts, quantiles and zero-mean CV.
- Symmetry, scale invariance, monotone-response bound and extreme Hill arguments.
- Exact interval endpoints and interior jumps.
- Invalid text/count/range rejection; 100,000-row calculation.

## UI and exports

- Default synthetic case, dark mode, visible limitations and provenance.
- Load each preset, return with reset, alter threshold and rerun.
- Alter analytical inputs: hide results and stale exports immediately.
- Mismatched units block comparison.
- Missing comparability or tail-resolution attestations block comparison.
- Empirical mode clears attestations and notes; missing notes block.
- Empty numeric field and unsupported censored/negative input block.
- JSON report retains numerical results, version, metadata and limitations.
- CSV sweep retains hypothetical field labels, units, slope and provenance.
- Local math and code links resolve.
- Theme toggle completes dark/light/dark cycle.
- Local CSV import bypasses the editor, clears synthetic assertions and retains filename provenance.
- File clear and preset reset restore editor inputs; oversize local files are blocked.
- Large calculation can be cancelled by editing or reset; no late result overwrites new inputs.

## Visual checks

- Desktop 1440×1000: initial results, charts, table and science.
- Mobile 390×844: controls, results, plots and science fit; no horizontal page overflow.
- 200%-equivalent narrow viewport with keyboard-operable controls.
- Error message remains visible, stale results hidden.
- Solid/dashed plus labels distinguish populations without color alone.
- Axis labels and zero-antigen case readable.

## Exclusions

No empirical biological validation, raw-FCS pipeline, calibration conversion, censoring reconstruction, clinical prediction, patent clearance or equivalence inference is tested or claimed.
